# Core — C layer

Formal ontologies. Typed entities, machine-readable.

- `wordpress/` — WordPress platform ontology (114 entities, v0.2)
- `design-systems/material3/` — M3 design system (273 token entities)
- `design-systems/` (future: fluent, carbon, cupertino)
- `federation/activitypub/` — placeholder (future)

**Rule**: design system swaps in core, binding swaps in bindings/. Platform ontology is invariant under design swap.
