# v3.6.12 Wave 2B-3 Date+Time #22+#23 - Phase 1 Diagnostic Inventory

Status: Phase 1 diagnostic submitted for review
Mode: Lock 5 diagnostic-first, no implementation
Selected route: Route A - bounded Date+Time completion inside the existing module

## Verdict

Proceed to Phase 2 with Route A, subject to review approval.

The existing `date-time/` module can be completed without touching baseline CSS,
closed providers, WordPress/Pilot files, plugin bindings, or lock files.

The critical Phase 1 finding is that the old v3.4.7 audit and BACKLOG #19 are
partly stale relative to current HEAD:

- Current code already has `role="grid"` on the date grid.
- Current code already generates `role="gridcell"` cells.
- Current code already has roving `tabIndex`.
- Current code already implements ArrowLeft / ArrowRight / ArrowUp / ArrowDown.
- Current code still lacks `role="row"`, `aria-current="date"`, Home / End,
  PageUp / PageDown, Shift+PageUp / Shift+PageDown, explicit Enter / Space
  commit documentation, and live month/year announcements.

The provider relationship is also clarified:

- Current `date-time/` does not call `window.labPopover`.
- Current pattern HTML does not use `[data-popover-trigger]`.
- Current pattern HTML does not load `modules/popover/*`.
- The matrix `popover/` consumer note is aspirational/stale for v3.6.12.

Therefore Route D is not selected. Provider migration would be a future
stop-and-return route, not a Phase 2 implementation path.

## Lock 5 Compliance

No safe-shortcut exception was used.

Required diagnostic elements:

- Source inputs: v3.6.12 Phase 0 plan, v3.6.11 close state, BACKLOG #19,
  component coverage map, module status matrix, promotion criteria, public
  surface charter, current `date-time/` files, and current `components.css`.
- Baseline boundary: Date picker Chunk H3 and Time picker Chunk H4 in
  `stylesheets/components.css`; no baseline edits required.
- Provider boundary: `popover/`, `ripple/`, and `icon-system/` remain untouched;
  current module is self-contained rather than an active `popover/` consumer.
- Semantic boundary: Date grid a11y is in scope; Time picker role hygiene is in
  scope only as bounded local clarification; locale/timezone/range persistence,
  mobile full-screen, plugin binding, and WordPress editor behavior are out of
  scope.
- Route buckets: A-F evaluated below.
- Selected route: Route A.
- Rejected routes: B-F with evidence below.
- Write scope: existing `date-time/` files plus modern docs and Phase 2 report.
- Fences: listed below.
- Validation plan: Phase 3 QA matrix below.

## Phase 0 Review Carry-Forward

### P3-1 - Path Disambiguation

Phase 0 listed baseline stylesheet paths without the `stylesheets/` subdirectory.

Correct paths:

- `products/reference-implementations/axismundi-lab/stylesheets/components.css`
- `products/reference-implementations/axismundi-lab/stylesheets/blocks.css`

These are hard fences in v3.6.12.

### P3-2 - Route D Precondition

Route D means real `popover/` provider alignment or migration.

If Phase 2 needs any of these, stop and return for review:

- edit `modules/popover/*`
- add `data-popover-trigger` as the primary runtime contract
- add `window.labPopover` dependency
- change provider ownership for anchor, dismiss, outside click, Escape, focus
  restore, viewport collision, or open state

Route D is not a v3.6.12 implementation route.

### P3-3 - Provider Boundary Critical Fork

Evidence:

- `lab-date-time.js`: no `window.labPopover`, no `labPopover` API call.
- `lab-date-time-pattern.html`: no `[data-popover-trigger]`.
- `lab-date-time-pattern.html`: no `data-popover-wired`.
- `lab-date-time-pattern.html`: links to the popover pattern page only as
  related navigation, not as a runtime dependency.

Decision:

- Provider relationship is aspirational/stale for v3.6.12.
- Current Date+Time closure should preserve the self-contained lab module.
- Matrix alignment can be handled in close docs or future docs if needed; do not
  edit `popover/`.

### P3-4 - BACKLOG #19 Status

BACKLOG #19 is not fully closed by drift. It is partially drift-closed and
partially still active.

Phase 2 should close the remaining Date grid items if Route A is approved.

### P3-5 - Time Picker Semantic Role

Current state:

- Time panel host: `role="dialog" aria-modal="false"`.
- Time dial host: `role="listbox"`.
- Dial options generated by JS: `role="option"` and `aria-selected`.
- Hour/minute fields and AM/PM buttons use `aria-pressed`.
- Direct text input exists for typed time entry.

Decision:

- Do not convert Time picker to `radiogroup` in v3.6.12.
- Keep the current listbox-ish dial contract and document it explicitly.
- Phase 2 may add bounded keyboard QA / documentation for the current contract.
- Any larger Time picker APG redesign routes to a future follow-on.

### P3-6 - Legacy DATE-TIME-AUDIT.md Handling

Decision: preserve the legacy audit as provenance and supplement it with modern
docs.

Phase 2 should:

- keep `DATE-TIME-AUDIT.md` as the v3.4.7 extraction/provenance document
- add modern docs in the v3.6.8-v3.6.11 style:
  - `DATE-TIME-SPEC-AUDIT.md`
  - `DATE-TIME-MEASUREMENT-AUDIT.md`
  - `DATE-TIME-RUNTIME-AUDIT.md`
  - `DATE-TIME-WP-MAPPING.md`
- update the legacy audit only if a small addendum or verdict note is needed to
  route readers to the new docs

This keeps historical context intact.

## Current Local State

Existing module files:

| File | Lines | Role |
|---|---:|---|
| `lab-date-time.js` | 805 | Self-contained Date and Time benchmark runtime |
| `lab-date-time.css` | 453 | Lab-scoped benchmark layout and demo scaffolding |
| `lab-date-time-pattern.html` | 371 | Pattern page and interactive specimens |
| `docs/DATE-TIME-AUDIT.md` | 464 | v3.4.7 extraction/provenance audit |

No new module directory is needed.

This is not a blank-module creation cycle. It is the first existing-module
completion cycle in the v3.6.x Wave 2 sequence.

## Baseline Boundary

### Date Picker

Current `components.css` mapping:

- Chunk marker: `Chunk H3 - Date picker (visual structure only)` at line 5613.
- Formal legacy marker inside comments: `§33 Date picker`.
- Runtime selectors begin at `.ax-date-picker` line 5651.
- Key selector range: lines 5651-5818.

Key selectors:

- `.ax-date-picker`
- `.ax-date-picker__header`
- `.ax-date-picker__supporting`
- `.ax-date-picker__headline`
- `.ax-date-picker__nav`
- `.ax-date-picker__nav-label`
- `.ax-date-picker__weekdays`
- `.ax-date-picker__grid`
- `.ax-date-picker__cell`
- `.ax-date-picker__cell.is-today`
- `.ax-date-picker__cell.is-selected`
- `.ax-date-picker__cell.is-outside`
- `.ax-date-picker__cell.is-in-range`
- `.ax-date-picker__cell.is-range-start`
- `.ax-date-picker__cell.is-range-end`

### Time Picker

Current `components.css` mapping:

- Chunk marker: `Chunk H4 - Time picker (visual structure only)` at line 5825.
- Formal legacy marker inside comments: `§34 Time picker`.
- Runtime selectors begin at `.ax-time-picker` line 5873.
- Key selector range: lines 5873-6166.

Key selectors:

- `.ax-time-picker`
- `.ax-time-picker__supporting`
- `.ax-time-picker__inputs`
- `.ax-time-picker__field`
- `.ax-time-picker__separator`
- `.ax-time-picker__period`
- `.ax-time-picker__period-input`
- `.ax-time-picker__period-btn`
- `.ax-time-picker__dial`

Baseline decision:

- Do not edit `components.css`.
- Do not edit `blocks.css`.
- Treat `§33` / `§34` as legacy internal labels under Chunk H3/H4, not as
  current formal section numbers.

## Runtime Inventory

### Date Runtime

Current date runtime owns:

- single-date and range modes
- docked text-field open control
- hidden date surface
- calendar / month / year / input views
- date input parsing
- range start/end preview
- OK / Cancel commit behavior
- Escape close
- previous / next month buttons
- click selection
- ArrowLeft / ArrowRight / ArrowUp / ArrowDown movement among rendered date cells
- roving `tabIndex` among date cells
- forbidden-ancestor skip at initialization

Current date runtime does not fully own:

- `role="row"` wrapper structure
- `aria-current="date"` on today's cell
- `aria-labelledby` from grid to month/year label
- Home / End keyboard movement
- PageUp / PageDown keyboard movement
- Shift+PageUp / Shift+PageDown year movement
- live month/year announcements
- explicit Enter / Space documentation and tests for date commit
- full range-selection a11y model

### Time Runtime

Current time runtime owns:

- 12h / 24h format switching
- hour / minute active part switching
- AM / PM period switching in 12h mode
- dial rendering
- generated `role="option"` dial buttons
- `role="listbox"` dial host in pattern HTML
- typed input mode
- typed value validation
- OK / Cancel commit behavior
- Escape close
- forbidden-ancestor skip at initialization

Current time runtime does not attempt:

- radiogroup rewrite
- spinbutton rewrite
- native `input[type="time"]`
- locale-specific time formats
- timezone handling

## BACKLOG #19 Item-by-Item Status

| BACKLOG #19 item | Current HEAD status | Phase 2 decision |
|---|---|---|
| `role="grid"` on calendar table | Present on `[data-date-grid]` in pattern HTML | Already drift-closed |
| `role="row"` on each week row | Not present; grid is a flat button list | Close or document as intentional if row wrappers are not added |
| `role="gridcell"` on each day cell | Present in JS-generated buttons | Already drift-closed |
| `aria-current="date"` on today's cell | Not present | Close in Route A |
| `aria-selected` on active selection | Present on generated cells | Already drift-closed |
| `aria-readonly` / `aria-multiselectable` as appropriate | Not present | Evaluate in Phase 2; `aria-multiselectable` may be appropriate for range mode |
| `aria-labelledby` pointing to month/year nav label | Not present | Close in Route A |
| ArrowLeft / ArrowRight | Present | Already drift-closed |
| ArrowUp / ArrowDown | Present | Already drift-closed |
| Home / End | Not present | Close in Route A |
| PageUp / PageDown | Not present | Close in Route A |
| Shift+PageUp / Shift+PageDown | Not present | Close in Route A |
| Enter / Space select date | Native button activation likely works | Verify and document in Phase 3 |
| Escape close picker | Present | Already drift-closed |
| one `tabindex="0"` at a time | Present via JS `button.tabIndex` | Already drift-closed |
| focus follows arrow-key navigation | Present for arrow keys | Already drift-closed |
| month/year changes announced | Not present | Close with live region or document if out of scope |
| `aria-live="polite"` region | Not present | Close in Route A |

Conclusion:

- BACKLOG #19 is narrowed by current drift but not closed.
- Route A should close the remaining Date grid a11y items without reopening
  Time picker, provider, plugin, or baseline territory.

## Responsibility Map

### In Scope for Route A

- Date grid a11y completion for the current lab module.
- Time picker contract documentation and bounded smoke tests.
- Modern audit docs for Date+Time.
- Pattern page keyboard instructions and visible a11y status update.
- Phase 3 QA for date keyboard, time selection, range preview, and forbidden
  ancestor behavior.

### Explicitly Out of Scope

- `popover/` provider migration.
- `ripple/` or `icon-system/` edits.
- `components.css` or `blocks.css` edits.
- plugin-tier date bindings.
- WordPress editor sidebar date control.
- post meta binding.
- ActivityPub Event objects.
- timezone handling.
- locale calendar systems.
- recurring events.
- mobile full-screen picker variant.
- full range-selection a11y redesign beyond current preview and selection
  behavior.

## Route Selection

### Route A - Full Date+Time Completion

Selected.

Reasons:

1. Existing module is local and self-contained.
2. Baseline Date/Time primitives already exist and need no edits.
3. Provider migration is not needed to close the active gaps.
4. BACKLOG #19 is now narrow enough to complete locally.
5. Time picker has a current listbox/option contract that can be documented
   without broader APG redesign.
6. Modern docs can supplement the legacy v3.4.7 audit without losing provenance.
7. No stop-and-return trigger is active.

### Route B - Date Grid A11y First

Rejected as the final route but absorbed into Route A.

The Date grid a11y work is the main implementation center of Route A. A separate
Route B would leave the combined #22+#23 PARTIAL status unresolved even though
Time picker can be bounded to documentation and QA.

### Route C - Documentation / Audit Completion Only

Rejected.

Active #19 gaps remain: `aria-current`, Home/End, PageUp/PageDown,
Shift+PageUp/Down, live announcements, and grid labeling. Documentation-only
would be honest but would not complete the cycle's primary candidate.

### Route D - Popover Consumer Alignment

Rejected for v3.6.12 implementation.

The current module is not a real `popover/` consumer. Migrating it to one would
touch a closed provider-consumer boundary and activate Lock 5 stop-and-return.

Route D may be reopened later only with a fresh Phase 0/1 plan.

### Route E - Split / No-Code Routing

Rejected.

The remaining implementation is bounded enough for one cycle. Keep range
selection a11y, locale/timezone, plugin binding, and mobile full-screen variants
as explicit non-goals rather than splitting the current #19 work.

### Route F - Other

Rejected. No evidence supports another route.

## Phase 2 Write Scope

Expected implementation files:

- `products/reference-implementations/axismundi-lab/modules/date-time/lab-date-time.js`
- `products/reference-implementations/axismundi-lab/modules/date-time/lab-date-time.css`
- `products/reference-implementations/axismundi-lab/modules/date-time/lab-date-time-pattern.html`
- `products/reference-implementations/axismundi-lab/modules/date-time/docs/DATE-TIME-AUDIT.md`
- `products/reference-implementations/axismundi-lab/modules/date-time/docs/DATE-TIME-SPEC-AUDIT.md`
- `products/reference-implementations/axismundi-lab/modules/date-time/docs/DATE-TIME-MEASUREMENT-AUDIT.md`
- `products/reference-implementations/axismundi-lab/modules/date-time/docs/DATE-TIME-RUNTIME-AUDIT.md`
- `products/reference-implementations/axismundi-lab/modules/date-time/docs/DATE-TIME-WP-MAPPING.md`
- `docs/v3.6.12/WAVE-2B-DATE-TIME-PHASE-2-REPORT.md`

Expected implementation constraints:

- No `popover/` dependency added.
- No provider files edited.
- No baseline CSS edited.
- No plugin or WordPress binding added.
- Keep all behavior inside `modules/date-time/`.
- Keep legacy `DATE-TIME-AUDIT.md` primarily as provenance; add an addendum or
  cross-reference rather than rewriting history.

## Phase 2 Implementation Targets

Date grid:

- Add missing `aria-current="date"` for today's cell.
- Add an accessible month/year label relationship for the grid.
- Add Home / End movement.
- Add PageUp / PageDown movement.
- Add Shift+PageUp / Shift+PageDown movement.
- Add or expose a polite live announcement for month/year changes.
- Decide and document whether `role="row"` wrappers are implemented or omitted
  with rationale.
- Decide and document whether `aria-multiselectable` is used in range mode.
- Preserve existing Arrow key and roving tabindex behavior.

Time picker:

- Preserve current `role="listbox"` / `role="option"` dial contract.
- Do not convert to radiogroup/spinbutton/native time input.
- Add audit documentation that this is the current v3.6.12 contract and that
  deeper Time APG redesign is a future route if needed.

Pattern/docs:

- Replace stale warning copy that says all #19 items are missing.
- Add keyboard instructions for the Date grid.
- Add modern audit docs.
- Add a legacy audit addendum pointing to v3.6.12 completion docs.

## Files Still Not Expected To Change

Lock and process:

- `AGENTS.md`
- `CLAUDE.md`

WordPress / Pilot:

- `products/reference-implementations/axismundi-pilot/theme.json`
- `products/reference-implementations/axismundi-pilot/functions.php`
- `products/reference-implementations/axismundi-pilot/bridge/pilot-block-bridge.css`
- `products/reference-implementations/axismundi-pilot/bridge/pilot-block-bridge.js`
- `products/reference-implementations/axismundi-pilot/assets/styles/pilot-block-bridge.css`
- `products/reference-implementations/axismundi-pilot/assets/scripts/pilot-block-bridge.js`
- `products/reference-implementations/axismundi-pilot/fixtures/core-block-specimen-wall.html`
- `products/reference-implementations/axismundi-pilot/fixtures/core-block-editor-smoke.html`

Baseline / styleguide:

- `products/reference-implementations/axismundi-lab/stylesheets/components.css`
- `products/reference-implementations/axismundi-lab/stylesheets/blocks.css`
- `products/reference-implementations/axismundi-lab/style-guide.html`
- `products/reference-implementations/axismundi-lab/scripts/style-guide.js`
- `products/reference-implementations/axismundi-lab/scripts/theme.js`

Providers:

- `products/reference-implementations/axismundi-lab/modules/popover/`
- `products/reference-implementations/axismundi-lab/modules/ripple/`
- `products/reference-implementations/axismundi-lab/modules/icon-system/`

Closed modules:

- `products/reference-implementations/axismundi-lab/modules/app-bar/`
- `products/reference-implementations/axismundi-lab/modules/nav-bar/`
- `products/reference-implementations/axismundi-lab/modules/nav-rail/`
- `products/reference-implementations/axismundi-lab/modules/tabs/`
- `products/reference-implementations/axismundi-lab/modules/menu/`
- `products/reference-implementations/axismundi-lab/modules/checkbox/`
- `products/reference-implementations/axismundi-lab/modules/radio/`
- `products/reference-implementations/axismundi-lab/modules/switch/`
- `products/reference-implementations/axismundi-lab/modules/dialog/`
- `products/reference-implementations/axismundi-lab/modules/sheet/`

Other existing modules:

- `products/reference-implementations/axismundi-lab/modules/text-field/`
- `products/reference-implementations/axismundi-lab/modules/search-bar/`
- `products/reference-implementations/axismundi-lab/modules/button/`
- `products/reference-implementations/axismundi-lab/modules/button-group/`
- `products/reference-implementations/axismundi-lab/modules/fab/`

Validators / generators:

- `tools/validators/validate_theme_pilot.py`
- `tools/validators/validate_pilot_specimen_wall.js`
- `tools/generators/build_pilot_specimen_wall.py`

## Stop-And-Return Triggers

Return for review before implementation if any of these become necessary:

1. `components.css` or `blocks.css` edits.
2. `popover/`, `ripple/`, or `icon-system/` edits.
3. new shared calendar, date, overlay, focus, or popover provider.
4. WordPress / Pilot / plugin binding.
5. AGENTS.md / CLAUDE.md or Lock 5 changes.
6. mobile full-screen Date/Time picker as mandatory closure criterion.
7. locale calendars, timezone, recurring event, ActivityPub, or post-meta
   behavior.
8. full range-selection a11y redesign beyond the current UI preview.

## Phase 3 QA Matrix

Visual:

- desktop light
- desktop dark
- mobile light at 390px
- mobile dark at 390px
- console errors 0
- horizontal overflow 0
- token color tuple unchanged from v3.6.11

Date:

- open date picker
- close with Escape
- close with Cancel
- OK commits selected date
- ArrowLeft / ArrowRight move focus
- ArrowUp / ArrowDown move focus
- Home / End move to row/week boundaries according to the implemented contract
- PageUp / PageDown change month
- Shift+PageUp / Shift+PageDown change year
- Enter / Space activate focused date
- `aria-current="date"` exists on today's cell
- selected cell exposes `aria-selected="true"`
- only one date cell has `tabindex="0"`
- live month/year announcement changes on month/year navigation
- forbidden ancestor specimen remains inert

Time:

- open time picker
- switch 12h / 24h
- switch hour / minute active part
- select dial option
- AM / PM switch in 12h mode
- typed input validates and commits
- Escape closes
- OK commits
- current `role="listbox"` / `role="option"` contract remains coherent

Fences:

- `popover/`, `ripple/`, `icon-system/` unchanged
- `components.css` / `blocks.css` unchanged
- WordPress / Pilot files unchanged
- lock files unchanged

Standard commands:

- `wp-env run cli wp core version`
- `python tools/generators/build_pilot_specimen_wall.py`
- `npm run validate:specimen-wall`
- `php -l products/reference-implementations/axismundi-pilot/functions.php`
- `npm test`
- `npm run validate:computed`
- `npm run publish:styleguide`
- `git diff --check`

## Expected Phase 5 Decisions

If Phase 2 and Phase 3 pass, Phase 5 should decide:

- Whether #22+#23 moves from PARTIAL to DONE.
- Whether BACKLOG #19 closes fully or narrows to any remaining date-grid item.
- Whether any Time picker follow-on is needed.
- Whether any Date+Time provider-matrix wording needs future correction.
- Whether the legacy `DATE-TIME-AUDIT.md` is sufficiently preserved by the new
  docs and addendum.
