# v3.6.15 VS Code Diagnostics Sweep - Phase 1 Report

Date: 2026-05-23

## Summary

```txt
Route:        Diagnostic-only sweep
Status:       CORRECTED
Recommendation: HOLD - corrected Phase 1 review required before Phase 2/5
```

The initial report treated repo-level parser and validator checks as the
diagnostic surface. User correction clarified that the intended diagnostic
surface was the VS Code Problems panel after opening lab module files. This
report is amended in-place: VS Code Problems panel diagnostics are primary
evidence; parser and validator checks are supporting evidence.

## Git Baseline

```txt
HEAD:        c1a1290 Close v3.6.14 wave 3 components
branch:      main == origin/main
status:      clean before diagnostics
```

Generated outputs from `npm test`, `python -m compileall`, and
`npm run publish:styleguide` were restored / removed after the sweep. The final
working tree returned to clean except for the new v3.6.15 docs.

## Scope Correction

```txt
Original user intent:
  VS Code Problems panel sweep for lab module files, opened in the editor and
  inspected with Ctrl+Shift+M.

Initial Codex scope:
  Repo-level diagnostics config inventory plus parser/validator sweep.

Correction:
  Add user-supplied VS Code Problems diagnostics as Primary Evidence.
  Reclassify the parser/validator sweep as Supporting Evidence.
```

The correction does not invalidate the parser/validator results; it changes
their role. They show the repository is syntactically and validator-clean under
CLI gates, while the VS Code Problems panel exposes editor-extension hints and
HTML/CSS/a11y diagnostics that the CLI gates do not cover.

## Primary Evidence - VS Code Problems Panel

Source:

```txt
User-captured VS Code Problems panel diagnostics after opening lab module files.
Sources include Microsoft Edge Tools, VS Code CSS language service, webhint,
and axe-style diagnostics.
```

Scope actually captured:

```txt
Full lab module surface, not only the v3.6.14 Wave 3 priority slice.
```

Wave 3 priority slice result:

```txt
slider/loading/progress source errors: 0
slider/loading/progress warnings:      9 no-inline-styles warnings
```

The Wave 3 warnings are generated by the shared module-page inline critical
style pattern near the top of each pattern HTML file. They are not v3.6.14
functional regressions.

Severity 8 diagnostics captured:

| File | Source | Code | Summary | Initial triage |
|---|---|---|---|---|
| `modules/button-group/lab-button-group.css:87` | Microsoft Edge Tools | `compat-api/css` | `inline-size: fit-content` Samsung Internet fallback hint | P3 compat hygiene |
| `modules/button/lab-button.css:131` | Microsoft Edge Tools | `compat-api/css` | `user-select` Safari prefix hint | P3 compat hygiene |
| `modules/date-time/lab-date-time.css:22-31` | VS Code CSS | parser errors | header comment contains nested `/* EXTRACTED */` marker, causing CSS parser confusion | P2 source hygiene |
| `modules/menu/lab-menu-pattern.html:77` | Microsoft Edge Tools / axe | `aria-allowed-attr` | `aria-selected` is not allowed on `role="menuitem"` | P2 a11y semantics |
| `modules/nav-bar/lab-nav-bar-pattern.html:81` | Microsoft Edge Tools / axe | `aria-allowed-attr` | `aria-selected` is not allowed on a plain button nav item | P2 a11y semantics |
| `modules/ripple/lab-ripple-pattern.html:190` | Microsoft Edge Tools / axe | `aria-required-parent` | standalone `role="menuitem"` specimen lacks required menu/menubar/group parent | P2 specimen semantics |

Other severity 4 diagnostics are dominated by:

```txt
no-inline-styles      repeated on module pattern critical style blocks
compat-api/css        color-mix, scrollbar-width, min-height:auto, user-drag
css-prefix-order      user-select / -webkit-user-select ordering hints
emptyRules            tooltip module empty-rule warning
```

These warning classes are broad hygiene signals, not Phase 1 blockers. They
should be routed or handled only after deciding whether Microsoft Edge Tools /
webhint compatibility rules are normative for lab validation surfaces.

## Supporting Evidence - Parser / Validator Sweep

## Diagnostics Inventory

Configuration inventory:

```txt
.vscode/                absent
tsconfig/jsconfig       absent
ESLint config           absent
Stylelint config        absent
pyproject.toml          absent
composer/phpcs config   absent
```

File counts checked:

```txt
JavaScript files checked: 25
PHP files checked:         8
Python files checked:     28
JSON files checked:       50
```

## Results

### JavaScript Syntax

Command:

```txt
node --check <each repo JS file outside node_modules/styleguide>
```

Result:

```txt
PASS - 25 / 25 files
```

### PHP Syntax

Command:

```txt
php -l <each repo PHP file>
```

Result:

```txt
PASS - 8 / 8 files
```

### Python Syntax

Command:

```txt
python -m compileall -q tools
```

Result:

```txt
PASS - tools Python modules compile
```

Cleanup:

```txt
Removed generated tools/**/__pycache__/ directories after the check.
```

### JSON Syntax

Initial PowerShell `ConvertFrom-Json` produced a false positive on
`package-lock.json` because PowerShell rejects an empty-string property name
unless `-AsHashTable` is used. The authoritative retry used Node `JSON.parse`.

Command:

```txt
Node JSON.parse <each repo JSON file outside node_modules>
```

Result:

```txt
PASS - 50 / 50 files
```

### Project Validator

Command:

```txt
npm test
```

Result:

```txt
PASS - Overall 1.000
Axis A schema:  1.000
Axis B theme:   1.000
Axis C css:     1.000
Axis D runtime: 1.000
Axis E tokens:  1.000
Axis F bridge:  1.000
Axis G custom:  1.000
```

Cleanup:

```txt
Restored generated bindings/wordpress-material3/binding_legitimacy_audit.json
Restored generated bindings/wordpress-material3/pilot_validation_report.md
```

### Published Mirror

Command:

```txt
npm run publish:styleguide
```

Result:

```txt
PASS - 53 files generated in publish surface
```

Cleanup:

```txt
Restored tracked styleguide/ mirror files.
Removed newly generated untracked styleguide/stylesheets/lab-*.css mirror files.
```

### Docker-dependent Validation Retry

Initial command:

```txt
npx wp-env start
```

Initial result before desktop reboot:

```txt
BLOCKED - Docker Desktop / wp-env unavailable
unable to get image 'mariadb:lts'
open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified
```

After desktop reboot and Docker Desktop launch:

```txt
npx wp-env start
PASS - WordPress development site started at http://localhost:8888
```

The fresh wp-env database was not installed, so the local validation fixture was
recreated:

```txt
wp core install --url=http://localhost:8888 --title=Axismundi \
  --admin_user=admin --admin_password=password --admin_email=admin@example.com
wp theme activate axismundi-pilot
wp post update 1 with the prose QA pattern fixture
wp post update 10 with the Axismundi Pattern QA fixture
```

Final commands:

```txt
python tools/generators/build_pilot_specimen_wall.py
npm run validate:specimen-wall
npm run validate:computed
```

Final result:

```txt
build_pilot_specimen_wall:
  PASS - created Axismundi Core Block Specimen Wall page 13
       - created Axismundi Core Block Editor Smoke page 14

validate:specimen-wall:
  PASS - specimen wall render gate PASS

validate:computed:
  PASS - computed-style audit PASS
```

Generated Pilot asset copies from `build_pilot_assets.py` were restored after
environment setup. The WordPress database fixture is local runtime state only
and is not committed.

### Whitespace / Git

Command:

```txt
git diff --check
git status --short --branch
```

Result after cleanup:

```txt
PASS - no whitespace errors
status - clean before adding v3.6.15 docs
```

## Findings

### P1

```txt
None.
```

### P2

```txt
P2-1 Scope mismatch corrected in this report.
     Primary evidence is now VS Code Problems panel diagnostics; parser and
     validator output is supporting evidence.

P2-2 Existing lab module Problems panel source diagnostics require route
     decision before close:
       - date-time CSS parser confusion from nested comment marker
       - menu aria-selected on role=menuitem
       - nav-bar aria-selected on plain button nav item
       - ripple standalone role=menuitem without required parent

     These are not v3.6.14 Wave 3 regressions, but they are the main findings
     surfaced by the intended VS Code Problems sweep. Phase 5 close should not
     proceed until reviewer decides whether v3.6.15 should fix these narrowly
     in Phase 2 or route them forward.
```

### P3

```txt
P3-1 No repo-level VS Code diagnostics configuration exists. If future cycles
     want exact editor diagnostics parity, add a dedicated diagnostics config
     in a separate plan-first cycle rather than treating this sweep as that
     implementation.

P3-2 Microsoft Edge Tools compatibility and webhint warnings are broad hygiene
     signals. Decide later whether they are normative for lab module pages,
     especially no-inline-styles warnings on intentionally self-contained
     pattern-page critical styles.
```

## Lock 1-5

```txt
Lock 1 Axis G: preserved by npm test Axis G 1.000.
Lock 2 Axis E: preserved by npm test Axis E 1.000.
Lock 3 core/button: no WordPress bridge implementation touched.
Lock 4 semantic rows: no row routing changed.
Lock 5 diagnostic-first: satisfied; this was a diagnostic-only cycle.
```

## Recommendation

```txt
HOLD - corrected Phase 1 review required

Phase 2 may be needed if the reviewer accepts narrow fixes for the P2 source
diagnostics. Otherwise, route the P2 items forward explicitly and close only
after that routing decision.

Docker-dependent validation debt is resolved for this local session.
```
