<?php
/**
 * Feed Item Templates server render.
 *
 * Nothing. This holds the cards the loop can repeat — one per density — and the loop extracts the
 * one the reader asked for. Rendering the children here would put every density on the page at
 * once, which is the opposite of choosing between them.
 *
 * @package AxismundiActivities
 */

defined( 'ABSPATH' ) || exit;
