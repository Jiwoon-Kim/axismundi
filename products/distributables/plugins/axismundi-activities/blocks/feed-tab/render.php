<?php
/**
 * Feed Tab server render.
 *
 * Nothing, for the same reason its parent renders nothing: this block's job is to be found, not
 * to draw. The feed reads the arrangement out of the tab whose surface is current and assembles
 * the parts itself, because the parts describe the query it just ran.
 *
 * @package AxismundiActivities
 */

defined( 'ABSPATH' ) || exit;
