<?php
/**
 * Feed Tabs server render.
 *
 * Nothing. This holds one layout per surface and the feed renders the one the reader is on —
 * rendering the children here would put every surface on the page at once, which is the opposite
 * of tabs.
 *
 * @package AxismundiActivities
 */

defined( 'ABSPATH' ) || exit;
