<?php
/**
 * Silent fallback index.
 *
 * Axismundi is a block theme: the site is rendered by the block templates in
 * `templates/`, not by PHP template files. WordPress requires every theme to ship
 * an `index.php` as the ultimate fallback, so this file exists to satisfy that
 * requirement and intentionally renders nothing on its own.
 *
 * @package Axismundi
 */

defined( 'ABSPATH' ) || exit;
