<?php
/**
 * Silent fallback index.
 *
 * Omphalos is a block theme: the site is rendered by the block templates in
 * `templates/` (this theme + the Twenty Twenty-Five parent), not by PHP template
 * files. WordPress requires every theme to ship an `index.php` as the ultimate
 * fallback, so this file exists to satisfy that requirement and intentionally
 * renders nothing on its own.
 *
 * @package Omphalos
 */

defined( 'ABSPATH' ) || exit;
