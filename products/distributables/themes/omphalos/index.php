<?php
/**
 * Silent fallback index.
 *
 * Omphalos is a block theme: the site is rendered by block templates, which
 * this theme inherits from its Axismundi parent and overrides by filename when
 * a layout experiment needs to. WordPress requires every theme to ship an
 * `index.php` as the ultimate fallback, so this file exists to satisfy that
 * requirement and intentionally renders nothing on its own.
 *
 * @package Omphalos
 */

defined( 'ABSPATH' ) || exit;
