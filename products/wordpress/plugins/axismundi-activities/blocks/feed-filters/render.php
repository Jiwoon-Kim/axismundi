<?php
/**
 * Feed Filters server render.
 *
 * Nothing, deliberately. The switches describe the same query the surrounding loop just ran —
 * which surface, which combination, which labels — and the loop emits them while it still holds
 * that answer. This block exists so an author can say where they go, and rendering them here as
 * well would put a second set of controls on the page disagreeing with the first.
 *
 * @package AxismundiActivities
 */

defined( 'ABSPATH' ) || exit;
